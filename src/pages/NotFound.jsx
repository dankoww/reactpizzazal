import React from 'react';
import NotFoundBlock from '../components/NotFoundInfo';

const NotFound = () => {
  return (
    <React.Fragment>
      <NotFoundBlock />
    </React.Fragment>
  );
};

export default NotFound;
